[phases.setup]
nixPkgs = ["nodejs_22", "npm-9_x"]

[phases.install]
cmds = ["cd backend && npm ci --ignore-scripts"]

[phases.build]
cmds = ["echo 'Build phase completed'"]

[start]
cmd = "cd backend && npm run validate-env && npm run migrate && npm start"
