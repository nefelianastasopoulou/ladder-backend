{
  "name": "ladder-app",
  "version": "1.0.0",
  "description": "Ladder app with backend and frontend",
  "private": true,
  "scripts": {
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
